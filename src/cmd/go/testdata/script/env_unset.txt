# Test that we can unset variables, even if initially invalid,
# as long as resulting config is valid.

env GOENV=badenv
env GOOS=
env GOARCH=
env GOEXPERIMENT=

! go env
stderr '^gecko: unknown GOEXPERIMENT badexp$'

go env -u GOEXPERIMENT

! go env
stderr '^gecko: unsupported GOOS/GOARCH pair bados/badarch$'

! go env -u GOOS
stderr '^gecko: unsupported GOOS/GOARCH pair \w+/badarch$'

! go env -u GOARCH
stderr '^gecko: unsupported GOOS/GOARCH pair bados/\w+$'

go env -u GOOS GOARCH

go env

-- badenv --
GOOS=bados
GOARCH=badarch
GOEXPERIMENT=badexp
