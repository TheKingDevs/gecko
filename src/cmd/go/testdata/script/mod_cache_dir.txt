# Go should reject relative paths in GKMODCACHE environment.

env GKMODCACHE="~/test"
! go install example.com/tools/cmd/hello@latest
stderr 'must be absolute path'

env GKMODCACHE="./test"
! go install example.com/tools/cmd/hello@latest
stderr 'must be absolute path'
