
# Tool name can't be empty. Issue #74757.
! go tool ''
stderr 'gecko: no such tool ""'

! go tool -n ''
stderr 'gecko: no such tool ""'

# Invalid tool name
! go tool @
stderr 'gecko: no such tool "@"'

! go tool -n @
stderr 'gecko: no such tool "@"'

-- go.mod --
module example.com/foo

go 1.24
-- main.go --
package main
