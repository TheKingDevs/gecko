# Test that experiments appear in "go version <binary>"

env GKEXPERIMENT=fieldtrack
go build -o main$GKEXE version.go
go version main$GKEXE
stdout 'X:fieldtrack$'
exec ./main$GKEXE
stderr 'X:fieldtrack$'

-- version.go --
package main
import (
	"go/version"
	"runtime"
)
func main() {
	if !version.IsValid(runtime.Version()) {
		panic("version not valid: "+runtime.Version())
	}
	println(runtime.Version())
}
