# https://golang.org/issue/43938: 'go build' should succeed
# if GKPATH and the variables needed for its default value
# are all unset but not relevant to the specific command.

env HOME=''
env home=''
env GKPATH=''

go list -deps main.go
stdout '^io$'

-- main.go --
package main

import _ "io"
